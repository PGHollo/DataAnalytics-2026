customer_id = 1001  # Assumption: this is a numeric ID, but it could be a string if it has leading zeros.
customer_name = "Jane Doe"  # Assumption: storing the full customer name in one string.
customer_gender = "Female"  # Assumption: gender is stored as text.
customer_date_of_birth = "1998-04-15"  # Assumption: stored as a string in YYYY-MM-DD format.
drivers_license_number = "D123456789"  # Assumption: stored as a string because it can contain letters and numbers.
auto_policy_number = "AUTO-987654"  # Assumption: stored as a string because policy numbers may include letters, numbers, or dashes.

my_name = "Patrick Green-Holloway"
hometown = "Baltimore, Maryland"

print(my_name)
print(hometown)


# Full list of Python reserved words / keywords:
# False, None, True, and, as, ^assert^, async, ^await^, break, class,
# continue, def, del, elif, else, except, finally, for, from,
# global, if, import, in, is, ^lambda^, nonlocal, not, or, pass,
# raise, return, try, while, with, ^yield^

# if: Used to start a condition. Python runs the code only if the condition is True.
# Example: if age >= 18:

# for: Used to repeat code for each item in a ^^sequence^^ like a list, string, or range.
# Example: for name in names:

# def: Used to create a function, which is a reusable block of code.
# Example: def greet_customer():

# return: Used inside a function to send a value back after the function runs.
# Example: return total_price

# import: Used to bring in outside code or tools from a module so you can use them.
# Example: import math